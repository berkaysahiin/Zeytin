module;

export module zeytin.shared.annotation;

export {
	enum class AnnotationKey {
		NAME,
		TOOLTIP,
		MIN,
		MAX,
		GROUP,
		HIDDEN,
		SAVE,
		READONLY,
		FILEPATH,
		ENABLE_IF,
		DEPRECATED,
		HELPER,
		UNKNOWN
	};
}
